# Frontend

This folder will contain the React + Vite client for Insight Engine.

The frontend is expected to stay thin:
- upload dataset
- capture natural language questions
- display returned Plotly figures
- show metadata and session state
